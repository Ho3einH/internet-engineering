# خلاصه و ساختار فصل سوم: لایه IP در شبکه اینترنت

## 📡 مفاهیم و ساختار پروتکل IP

لایه IP وظیفه هدایت بسته های اطلاعاتی بین شبکه ها را بر عهده دارد و مستقل از نوع شبکه فیزیکی عمل می‌کند.

آدرس‌های MAC برای شناسایی در شبکه‌های محلی به کار می‌روند، اما آدرس IP برای شناسایی یکتا و جهانی ماشین‌ها در اینترنت است.

بسته‌های IP (دیتاگرام‌ها) قالب مشخصی دارند که شامل فیلدهای نسخه، طول، نوع سرویس، شناسه، زمان زندگی (TTL)، پروتکل بالاتر و چک‌سام است.

مسیریاب‌ها (Router) بر اساس آدرس IP مقصد، بهترین مسیر را برای ارسال بسته‌ها انتخاب می‌کنند.

زیرشبکه‌ها (Subnet) بخش‌هایی از شبکه بزرگ‌تر هستند که ارتباطات داخلی و خارجی را مدیریت می‌کنند و باعث بهینه‌سازی آدرس‌دهی می‌شوند.

## 🌐 آدرس‌دهی IP و کلاس���های آدرس

آدرس IP شامل 32 بیت است و به چهار بخش ده‌دهی با نقطه جدا می‌شود.

کلاس‌های آدرس IP شامل A، B، C، D (چندپخشی) و E (رزرو شده) هستند که هر کدام دامنه و کاربرد خاص خود را دارند.

کلاس A برای شبکه‌های بزرگ، کلاس B برای شبکه‌های متوسط، کلاس C برای شبکه‌های کوچک و پرکاربرد است.

آدرس‌های خاصی مانند 0.0.0.0، 255.255.255.255 و 127.x.x.x کاربردهای ویژه مانند آدرس‌دهی پیش‌فرض، پخش سراسری و آدرس بازگشت (loopback) را دارند.

## 🛠 پروتکل‌های کمکی در لایه IP

ICMP برای گزارش خطاهای شبکه و اطلاع‌رسانی به مبدا بسته در صورت بروز مشکلات استفاده می‌شود.

ARP وظیفه تبدیل آدرس IP به آدرس فیزیکی (MAC) را در شبکه محلی انجام می‌دهد.

RARP برعکس ARP عمل می‌کند و برای دریافت آدرس IP از روی آدرس فیزیکی به کار می‌رود.

BOOTP و DHCP پروتکل‌هایی برای تخصیص خودکار آدرس IP و پیکربندی شبکه به صورت پویا هستند.

## 🔍 اجزای کلیدی بسته IP

فیلد Version مشخص‌کننده نسخه پروتکل (IPv4 یا IPv6) است.

فیلد IHL طول هدر IP را تعیین می‌کند.

فیلد Type of Service اولویت و کیفیت سرویس را مشخص می‌کند.

فیلد Total Length طول کل بسته را نشان می‌دهد.

فیلد Identification، Fragment Offset و بیت‌های DF و MF برای مدیریت قطعه‌بندی بسته‌ها کاربرد دارند.

فیلد TTL مشخص‌کننده طول عمر بسته و جلوگیری از گردش بی‌پایان آن در شبکه است.

فیلد Checksum جهت تشخیص خطا در هدر به کار می‌رود.

آدرس‌های مبدأ و مقصد در فیلدهای Source و Destination Address قرار دارند.

## 🚦 کاربرد و اهمیت پروتکل ICMP

ICMP پیام‌های مختلفی برای تشخیص خطا و مدیریت ترافیک ارسال می‌کند مانند Destination Unreachable، Time Exceeded، Redirect و Echo Request/Reply (پینگ).

این پروتکل به مسیریاب‌ها و دستگاه‌ها کمک می‌کند تا وضعیت شبکه را تشخیص داده و بهینه‌سازی کنند.

